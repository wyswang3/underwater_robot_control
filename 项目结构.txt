underwater_robot_control/
├── data/                     # 数据存储
│   ├── raw/                  # 原始数据
│   ├── processed/            # 预处理后的数据
│   └── splits/               # 训练/验证/测试集划分
├── models/                   # 模型定义
│   ├── dynamics_net.py       # 动力学网络模型
│   └── checkpoints/          # 保存训练后的模型
├── utils/                    # 工具函数
│   ├── preprocessing.py      # 数据预处理
│   ├── filters.py            # 滤波工具
│   ├── visualization.py      # 可视化工具
│   └── dataset.py            # 加载预处理数据并恢复原始窗口模式
    └── training.py           # 训练函数与模型评估函数设计
    └── predict_and_compare_lstm # 加载训练好的模型并记录拟合误差值
├── config.py                 # 配置文件
├── train.py                  # 训练脚本
├── evaluate.py               # 评估脚本
└── requirements.txt          # 依赖库

# 激活服务器虚拟环境：conda activate /home/furui/pzy/wys_lstm/wyswang3_env
#conda deactivate
#git checkout -b feature/pure-lstm
#git status
#git add .
#git commit -m "调整了网络模型的加载预测绘图标注"
#git push -u origin feature/pure-lstm

设计新的分支：
git fetch origin                         # 拉取最新远程分支
git checkout -b feature/pure-lstm-mlp    # 本地新建并切换分支
git add .                                # 添加所有文件改动
git commit -m "Refactor dynamics net to use MLP thrust mapping and update dataset/evaluate modules"
git push --set-upstream origin feature/pure-lstm-mlp
